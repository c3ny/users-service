services:
  users-service:
    container_name: users-service
    image: users-service:1.0.0
    build:
      context: .
      target: development
      dockerfile: ./Dockerfile
    command: npm run start
    ports:
      - 3002:3002
    volumes:
      - .:/app
    restart: unless-stopped
    env_file:
      - .env
  postgres_users_service:
    container_name: postgres-users-service
    image: postgres:16
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USERNAME}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DATABASE}
    ports:
      - 5432:5432
    volumes:
      - postgres_users_service_data:/var/lib/postgresql/data

volumes:
  postgres_users_service_data:

configs:
  httpd-config:
    external: true
